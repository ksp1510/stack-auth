# SolidJS (TS) — stack-auth

- Vite + SolidJS (TypeScript)
- Auth0 Universal Login (redirect flow)

## Dev
```bash
pnpm -C ../../ dev
# or:
pnpm -C ../../ --filter solid-ts dev
```

Runs at http://localhost:5185
